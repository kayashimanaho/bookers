<h1>Books</h1>

<% @books.each do |book| %>

  <p>title</p>
   <span>
     <%= link_to book.title, books_path(book.id) %>
   </span>
   
   
  <p>body</p>
   <span>
     <%= link_to book.title, books_path(book.id) %>
   </span>
<% end %>  

<h2>New book</h2>
<%= form_with model:@book do |f| %>
  <h4>title</h4>
  <%= f.text_field :title %>
  
  <h4>Body</h4>
  <%= f.text_area :body %>
  
  <%= f.submit 'Create Book' %>
<% end %>